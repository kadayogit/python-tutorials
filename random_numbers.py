# python don't have random function, but its built-in module library, so you can import
import random

# Import the random module, and display a random number between 1 and 9:
print(random.randrange(1, 10))